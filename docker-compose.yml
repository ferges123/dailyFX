services:
  api:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "8438:8438"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request, os, time; urllib.request.urlopen('http://localhost:8438/api/health'); f='/data/scheduler.health'; assert os.path.exists(f) and time.time()-os.path.getmtime(f)<120"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  web:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "8439:80"
    depends_on:
      api:
        condition: service_healthy
